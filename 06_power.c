// Assignment 6: Power of a Number
#include <stdio.h>

int power(int base, int exponent);

int main() {
    int base, exp;
    printf("Enter base and exponent: ");
    scanf("%d %d", &base, &exp);
    // Call power function and print result
    return 0;
}

int power(int base, int exponent) {
    // Write logic here
}
