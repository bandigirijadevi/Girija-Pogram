// Assignment 11: String Length without strlen()
#include <stdio.h>

int stringLength(char str[]);

int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);
    // Call function and print length
    return 0;
}

int stringLength(char str[]) {
    // Write logic here
}
