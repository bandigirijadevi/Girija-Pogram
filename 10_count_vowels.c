// Assignment 10: Count Vowels in a String
#include <stdio.h>

int countVowels(char str[]);

int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);
    // Call function and print vowel count
    return 0;
}

int countVowels(char str[]) {
    // Write logic here
}
