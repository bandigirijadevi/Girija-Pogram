#include <stdio.h>

long factorial(int n);

int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    // Call function and print result
    printf("Factorial is %d", factorial(int n);
    return 0;
}

long factorial(int n) {
    // Write logic
    //
    int i;{
    for(i = 0; i <= n; i++)
result = 1;
}
return result;
}

