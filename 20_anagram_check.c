// Assignment 20: Check if Two Strings are Anagrams
#include <stdio.h>

int isAnagram(char str1[], char str2[]);

int main() {
    char str1[100], str2[100];
    printf("Enter first string: ");
    gets(str1);
    printf("Enter second string: ");
    gets(str2);
    // Call function and print result
    return 0;
}

int isAnagram(char str1[], char str2[]) {
    // Write logic here
}
