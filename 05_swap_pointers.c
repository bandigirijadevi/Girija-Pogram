// Assignment 5: Swap Two Numbers using Pointers
#include <stdio.h>

void swap(int *a, int *b);

int main() {
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    // Call swap function and print values before/after
    return 0;
}

void swap(int *a, int *b) {
    // Write logic here
}
